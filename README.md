# BopimoWebBuilds
Web Builds of Bopimo. There is the prerelease folder which contaisn buggy/incomplete builds, the Builds folder that contains each client, and just the misc folder which has like random things
